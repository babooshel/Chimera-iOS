name: Chimera
options:
  # Setting this to 12.0 forces the oldest possible project spec (Version 53)
  xcodeVersion: "12.0"
  deploymentTarget:
    iOS: "16.0"
targets:
  Chimera:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - Chimera
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.baboo.chimera
      INFOPLIST_FILE: Chimera/Info.plist
      GENERATE_INFOPLIST_FILE: NO
