name: Chimera
targets:
  Chimera:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources: [Chimera]
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.chimera.ios
      IPHONEOS_DEPLOYMENT_TARGET: 16.0
